type Game {
  name: String!
  minAge: Int!
  minPlayer: Int!
  maxPlayer: Int!
  category: String!
  genre: String!
  description: String!
}

input CreateGameInput {
  name: String!
  minAge: Int!
  minPlayer: Int!
  maxPlayer: Int!
  category: String!
  genre: String!
  description: String!
}